# NER_with_custom_data

Steps to run the project :-
1: bash init_setup.sh in bash script
2: Run "python setup.py install" in terminal (Use command "rm -f project_dir" to remove a env file created bymistake)
3: 